// 全体としてはsc
import Auth from "../components/auth"// cc  

export default function AuthPage() {
    return (
        <main
            className="flex flex-col items-center justify-center h-[calc(100vh-56px)])]"
        >
            <Auth />
        </main>
    )
}